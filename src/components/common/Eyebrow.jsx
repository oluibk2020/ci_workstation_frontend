export default function Eyebrow({ children }) {
  return (
    <p className="font-mono-tight text-xs font-semibold uppercase tracking-[0.15em] text-[var(--color-accent)]">
      {children}
    </p>
  );
}
